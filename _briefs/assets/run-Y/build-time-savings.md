# Run Y build notes: tools/time-savings/index.html

Source: Master Kit `06 Calculators and Tools (NEW Aug 2026)/Atlas_One_Time_Savings_Discovery.html` (after the Run W dash pass).
Changes on the hosted copy only (the kit file is untouched):
1. Google Fonts `<link>` removed; the two DM Sans faces (400, 700) embedded as base64 exactly as in `tools/self-assessment/index.html`. Horas was already embedded. No external script, style, font or image; the only outbound URLs are the booking link and the webhook.
2. Brand shell: the hosted `.hbar` (white logo, eyebrow "Time and Cost Savings Discovery") replaces the tool's own A1 mark row; Print / Save as PDF button kept; `.cta-band` (Book the 30 minute Back Office Audit, `book-david`) and `.afoot` footer added, same CSS as the self-assessment.
3. "Keep a copy of these results" card: name, company, email, phone; `send()` POSTs to the same GHL inbound webhook with the same field names as the other hosted tools (`first_name`, `last_name`, `phone`, `email`, `company`, `atlas_tool`, `atlas_headline`, `atlas_results_text`, `atlas_source_url`, `atlas_generated_at`) plus `hours_saved_year`, `time_savings_year`, `software_savings_year`, `total_savings_year`, `pay_runs_year`, `hourly_value`. Email or phone required. `mode:"no-cors"`, same as the others.
4. Audience: the kit copy is written for David in a meeting ("Fill this in with the prospect", "their business"); the hosted page is linked from prospect emails, so those six phrases now speak to the owner ("Fill this in for your business", "your time", "your business", "you could drop", headline "What are you still doing by hand?", the "How to use it" note). Numbers, tasks, defaults and the calculation are unchanged.
5. Mobile: under 560 px the two tables stack (task name full width, then hours / frequency / hrs per year / remove on one line, with small labels); scrollWidth 390 at 390 px.
Checks: no JS errors at 390 or 1440; zero network requests on load; sample (rate 40, first task 2 hrs per pay run, first subscription $120/mo) gives 264 hrs, $10,560, $1,440, $12,000 (hand-checked); webhook POST intercepted locally (aborted, not sent) and the payload keys read back.
