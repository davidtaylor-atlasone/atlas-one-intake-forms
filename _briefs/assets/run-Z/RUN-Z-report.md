# RUN Z report (terminal B, files only)

Built 2026-09-12 by Claude Code, unattended, no GHL UI, no questions asked. Job: David's decisions on the Run W and Run Y questions, seven items, then the Portal rebuild. Brief with progress log at `repo:_briefs/RUN-Z-terminal-B.md`; scripts, captures and renders in `repo:_briefs/assets/run-Z/`. Nothing sent, nothing deployed beyond the normal Pages push, nothing deleted (every file changed in the kit was copied to `OneDrive:_to_delete/superseded-2026-09-12/` first).

Paths are relative to `HR_Docs/Atlas_One_Master_Kit/06 Calculators and Tools (NEW Aug 2026)/` unless marked `repo:` (atlas-one-intake-forms, main, pushed, last commit below), `OneDrive:` (the OneDrive-AtlasOneSolutions root) or `_INTERNAL/`.

## What was done

1. **"2025 to 2026" in full.** Six occurrences in five files: `Business Value Diagnostic (Atlas One).html` (2), `Business Value Diagnostic (Espanol).html` (2), `Retention Cost Calculator (Atlas One).html` (1), `Retention Cost Calculator (Espanol).html` (1) and `repo:tools/retention-cost/index.html` (1). No "2025 to 26" is left anywhere in the tools folder or the repo tools. The repo retention copy is still byte identical to the kit copy; live `/tools/retention-cost/` returns 200 and serves the pushed file.
2. **Duplicate Savings Summary retired.** `Atlas One — Your Savings Summary.html` (byte identical to `Atlas One Savings Summary (Atlas One).html`, checked with cmp before the move) is now at `OneDrive:_to_delete/superseded-2026-09-12/Atlas One — Your Savings Summary.html`. No repoint was needed: the Master Hub card ("Savings Summary (client dashboard)"), the Tools Hub card ("Your Savings Summary") and `_INTERNAL/build_portal.py` line 33 all already pointed at the "(Atlas One)" file, and a search of the whole Marketing tree (HTML, MD, PY, JSON, outside `_to_delete`, `_gold_backup` and `_BUILD-LOG`) finds no reference to the em dash file name. The tools folder now has one Savings Summary.
3. **Portal title.** `_INTERNAL/build_portal_single.py` line 93: `<title>Atlas One Portal: build {{STAMP}}</title>`. Only that line changed in the builder.
4. **"Guaranteed rates"** in the Employee Benefits Options presenter: left as is, as decided.
5. **Kit Time Savings file, stacked mobile table.** `Atlas_One_Time_Savings_Discovery.html` now carries the hosted copy's mobile CSS as an additive `<style id="a1mobile-2026-09-12">` block after its own style block: under 560 px the two tables stack (task name full width, then hours / frequency / hours per year / remove with small labels), the hero and results tighten. The block is character for character the one in `repo:tools/time-savings/index.html`. No copy changed: "Fill this in with the prospect", "their time", "their business" and "How to use this in a meeting" all still read for the meeting. scrollWidth is 390 at 390 px (was 391) and 1440 at 1440 px; the Run Y sample (rate 40, first task 2 hours per pay run, first subscription $120) gives 264 hours, $10,560, $1,440, $12,000 before and after, byte identical capture. Looked at: `repo:_briefs/assets/run-Z/shots/Atlas_One_Time_Savings_Discovery_390.png`.
6. **Tools index header bar.** `repo:tools/index.html` has the same navy `.hbar` as the hosted tools: navy bar, periwinkle bottom rule, white logo (30 px, same `filter` as the tools), sticky, eyebrow "FREE BUSINESS TOOLS" at the right, and the bar sits outside `.wrap` so it spans the page like the tools' bars. The old plain logo row (white logo on the off-white page, Run Y question 3) is gone. Pushed (`217948e`); `https://forms.atlasonesolutions.com/tools/` returns 200 and the served file is byte identical to the pushed one (checked after Pages redeployed). Looked at: `repo:_briefs/assets/run-Z/shots/index_1440_top.png`, `index_390.png`. Copy from before in `repo:_briefs/assets/run-Z/tools-index-before.html`.
7. **Mobile pass on the six tools.** Files: `Atlas_One_Onboarding_Tracker.html`, `Employee Benefits Options (Atlas One).html`, `Employee Handbook Builder (Bilingual 50-State).html`, `NDA Builder (Atlas One).html`, `Safety Manual Builder (Bilingual OSHA).html`, `W-2 At-Will Employment Agreement Builder (Atlas One).html`. Backups (byte identical to the files before the pass) in `OneDrive:_to_delete/superseded-2026-09-12/tools-before-mobile-pass-run-Z/` (the Time Savings backup is there too).
   - Method: `repo:_briefs/assets/run-Z/mobilepass.py` inserts one `<style id="a1mobile-2026-09-12">` block right before `</head>` in each file. Additive only: a Python check strips the block back out of each patched file and gets the backup byte for byte. No markup, no JavaScript, no existing CSS rule was edited.
   - What each block does (all under 700 px, the presenter under 560 px to match its own query):
     - Onboarding Tracker: the three tables (documents, calls, pricing lines) become stacked rows in bordered blocks; a document row shows label and key with the remove button top right, then status and "received" side by side, then Notes with a small label; a call row shows date and remove, then "What we covered"; a pricing line shows label and remove, then Monthly and Setup / one-time with labels. The four and three column field grids go to one column; transparent table inputs get a visible border. Cause was the 44 / 16 / 12 / notes / 4 percent table at 481 px.
     - Employee Benefits Options: the four rate tables (Redirect Basic, Plus, Premium, MEC) become stacked rows: plan name on its own line, then the four rate inputs in a two by two grid with "Employee", "Employee + Spouse", "Employee + Child(ren)", "Family" labels drawn by CSS (all four tables share those headers). Cause was the five column table with `white-space:nowrap` on the first cell.
     - NDA Builder and W-2 Agreement Builder: the two column workspace becomes one column that cannot exceed the screen (`minmax(0,1fr)`, the panes get `min-width:0`), pane and bar padding drops to 16 px, the segmented NDA type control wraps, the save / logo button groups wrap, the document preview gets 18 px side padding and the signature blocks stack. Both already had a 900 px query that stacked the panes, but the `1fr` column kept its content's minimum width (470 and 439 px).
     - Handbook Builder and Safety Manual Builder (same layout, same block): the top bar wraps and stops being sticky, the 390 px fixed form column becomes one full width column above the preview, the fixed `100vh` app height is released so the page scrolls normally, the preview page fills the width with 0.4 in side padding, the cover shrinks (28 pt title), the signature lines stack. Cause was `grid-template-columns:390px 1fr` plus the unwrapped top bar (855 and 833 px).
     - All six: text, date, number, email and tel inputs, selects and `.btn` buttons are at least 44 px tall under 700 px.
   - Measured in Chromium at 390 px (mobile, touch): `document.documentElement.scrollWidth` is **390 on all six** (before: 522, 413, 855, 470, 833, 439) and zero elements extend past the viewport (`culprits.mjs`). 1440 px: scrollWidth 1440, and the full page renders before and after are pixel identical on five files; the NDA render differs only in a 14 by 12 px patch at the pulsing "live preview" dot (an opacity animation caught at a different moment), nothing else.
   - Outputs and generated documents: one deterministic sample per tool (`sample.mjs`: every input, select, checkbox and textarea filled from Run W's fixed pattern, calculate / generate / preview buttons fired, never print, save, send, download or the build-for-me link, with the clock pinned so date stamps match), captured at 390 px and at 1440 px: every field value, the generated document's innerHTML (`#out` for the Handbook and Safety Manual, `#document` for the NDA, `#doc` / `.document` for the W-2 agreement), `body.textContent` and the tracker's `getState()` JSON. `compare.py`: all twelve captures byte identical before and after (`sample-before.json` / `sample-after.json`); a second before run was identical to the first, so the capture itself is deterministic. Zero JavaScript errors on load or during the sample, before and after.
   - Looked at: 390 px full renders of all six (`repo:_briefs/assets/run-Z/shots/*_390.png`, contact sheets `contact-390-A.png`, `contact-390-B.png`) and crops of the restructured regions after a fill (`repo:_briefs/assets/run-Z/crops/`, contact sheet `contact-crops.png`): tracker document rows and pricing card, rate table, handbook cover, W-2 terms table and signature block, NDA signature block.

## Portal

Rebuilt with the path argument from `_INTERNAL/build_portal_single.py`: **BUILD Sep 12, 2026 9:29 PM**, 38 tools, 10.35 MB, title `Atlas One Portal: build Sep 12, 2026 9:29 PM` (colon confirmed in the file and in the headless page title), hero chip carries the stamp, no errors on open. Checked inside the base64 payloads: the mobile block is present in the inlined Employee Benefits Options, Handbook, Safety Manual, NDA, W-2 Agreement and Retention Cost Calculator; "2025 to 2026" is in the inlined Retention Cost Calculator and Business Value Diagnostic. The retired Savings Summary is not in the Portal (it never was; the catalogue lists the "(Atlas One)" file).

## Repo

Commits on main, each pushed: brief (`RUN-Z-terminal-B.md`), `217948e` (tools index bar, retention 2026, run-Z scripts and renders), `3629d84` (items 1 to 7 progress, captures), then this report. `repo:_briefs/assets/run-Z/`: `mobilepass.py`, `sample.mjs`, `compare.py`, `culprits.mjs`, `overflow.mjs`, `shots.mjs`, `crops.mjs`, `ts-sample.mjs`, `sample-before.json`, `sample-after.json`, `ts-sample-before.json`, `ts-sample-after.json`, `tools-index-before.html`, `shots/`, `shots-before/`, `crops/`, and a copy of this report.

## Assumptions

1. Item 2: "repoint any Hub, Tools Hub or build_portal.py entry that used it" turned out to be none; the Hub, Tools Hub and builder were left untouched rather than edited for the sake of it.
2. Item 3: only the `<title>` was changed. The Portal's own note paragraph (line 277 of the builder, "All N tools live inside it — nothing loads from a folder...") still carries an em dash; it is visible copy on the Portal landing card but outside the decision, so it was left (question 1).
3. Item 6: the eyebrow on the index bar reads "Free business tools" (the tools' bars carry the tool name; the index has no tool name, and the h1 below it already says "Free business calculators"). The bar is sticky like the tools' bars.
4. Item 7: 700 px is the breakpoint (the presenter uses 560 px, its own existing breakpoint), the stacked-rows treatment is used for the tables (as Run V did) rather than a scroll box, and the 44 px touch rule follows Run V's convention but only under 700 px (no `pointer:coarse` clause), so desktop and iPad landscape rendering is unchanged.
5. Item 7: the sample is Run W's synthetic fill (every field), not a hand picked scenario; the byte identical comparison is on the generated document HTML, all field values, the page's text content and the tracker's saved state, at both widths.

## Not done / skipped

- Nothing in the brief was skipped.

## Flagged, not changed (for David)

1. **NDA Builder, pre-existing bug, both widths:** the logo upload control never lands in the form. Line 779 has `FORM_SEL = ".panel-form"` but the pane's class is `panel form` (`.panel.form`), so the selector matches nothing and the script falls back to a fixed white box at top right (`position:fixed;top:12px;right:12px`) that sits over the "Want it done for you?" banner and hides its "Book a call" button on desktop and the phone number on a phone. The Independent Contractor builder uses the same `.panel-form` selector and does have that class; the W-2 builder uses `.form-pane` and matches. Fix is one token in JavaScript (`.panel-form` to `.panel.form`), outside this run's additive-CSS rule, so it was not applied. Before and after renders showing it: `repo:_briefs/assets/run-Z/shots/nda-top-before-after.png`, `nda-1440-top.png`.
2. Neither `Atlas_One_Onboarding_Tracker.html` nor `Atlas_One_Time_Savings_Discovery.html` is in the Portal catalogue (`build_portal.py` SECTIONS) or the Tools Hub; the mobile fixes to both are in the kit files only until they are listed. The tracker is internal, so that may be intended.

## Questions for David

1. The Portal landing card's note ("All 38 tools live inside it — nothing loads from a folder, a server or the internet") still has an em dash, from the same builder as the title. Change it to a full stop or colon in `build_portal_single.py` line 277?
2. Apply the one-token NDA Builder fix above (`.panel-form` to `.panel.form`)?
3. Should the Time Savings Discovery (and the Onboarding Tracker) be added to the Portal and the Tools Hub, now that the kit copy of the Time Savings tool has the mobile layout?
