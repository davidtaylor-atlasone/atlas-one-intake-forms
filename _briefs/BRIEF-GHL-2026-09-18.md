# BRIEF-GHL (current run: Run BS, 2026-09-18 14:45). DONE: Parts 1 to 44 (Run BQ closed 41 to 44, audited PASS). THIS RUN: Parts 45 to 51, the booking question that keeps current clients out of the Audit emails, plus the offer expiry cleanup.

History for older runs is in this repo's `_briefs/` copies and in each `RUN-*-report.md`; it is not repeated
here so this brief stays short enough to read in one pass.

Terminal name: GHL. Browser work in GoHighLevel through the ghl-browser Playwright MCP only. Start at
app.ridethehightide.com/ (never a deep URL), navigate in app, one low stakes click first; if it does not land
after one Cmd+R, stop cleanly, write "needs Chrome restart" at the top of the report, and end. Never fork or
background (rule 44). Fresh navigation before trusting state (rule 42). Never touch a Publish or Draft switch;
David publishes. Branches do not merge (rule 35): duplicate the remaining chain into each branch. Send nothing
to a real contact, no SMS, no HIPAA, delete no files, spend nothing. No dashes in any copy. Every part ends
with a fresh navigation read back and a screenshot. Assets `_briefs/assets/run-BS/`.

Read first: `_BUILD-LOG/audit-client-template-id.md` (written by GHL-JOBS Run BR). It carries
`CLIENT_TEMPLATE_ID=6aad97b6cbcc9427cbd4f68b` (template `A1 | Client | more we handle`),
`CALL_PURPOSE_FIELD_ID=8htKmj1jyx7kJWFwIC4n`, `CALL_PURPOSE_KEY=contact.call_purpose` and an empty
`PULSE_ADMIN_URL`. If that file is missing, stop and write "waiting on GHL-JOBS Run BR".

Why: current clients book the same 30 minute calendar as Audit prospects and must never get the prep email or
a document request. David's decisions are in `_BUILD-LOG/audit-decisions-2026-09-18.md`. Route by a booking
question, with the `client-current` tag as a second guard.

## Part 45: the booking question
On the 30-Minute Back-Office Audit calendar only (not the 15, 45 or 60 minute calendars, not the group), open
the booking form and add one required question: "What is this call for?" with exactly three choices in this
order: "My Back Office Audit", "I am already a client", "Something else". Map it to the existing contact field
Call purpose (`contact.call_purpose`, id 8htKmj1jyx7kJWFwIC4n); do not create another field. Save, reload,
read back, screenshot. If this calendar's booking form cannot map a question to a custom field in this
account, say so plainly, add the question whichever way the form does allow, and report exactly what the
answer is stored as so Part 46 can branch on it.

## Part 46: guard the prep email
Open "Audit: prep email" (f9162dd7-692e-4f91-80af-76c4d034ccb6, built in Run BQ). Make the first action after
the trigger an If/Else named "Client?": contact has tag `client-current` OR Call purpose is
"I am already a client" goes straight to END; the None branch carries the existing Wait 10 minutes and the
prep email send. A current client must never receive the prep email, whatever they answer. Read back every
node. Leave it Draft.

## Part 47: the client email branch
Open "Booking: confirm and remind" (7200e594-0a7a-47cc-847e-cb8a8e39eb31; Run BQ restored it to one trigger,
Internal Notification, Email C1 booking confirmation, Add Tag booked, Wait until 24 hours before, Email 24h
reminder, Wait until 1 hour before, Email Talk in an hour, END). Do not disturb that chain. Add, after the
Add Tag booked step and before the first Wait until, an If/Else "Current client?": tag `client-current` OR
Call purpose is "I am already a client" sends Email template `A1 | Client | more we handle`
(id 6aad97b6cbcc9427cbd4f68b), From Name "David Taylor, Atlas One Solutions", From Email
David@AtlasOneSolutions.com, subject "Everything Atlas One handles for you now, {{contact.first_name}}"; the
None branch sends nothing extra. Because branches do not merge, the 24 hour and 1 hour reminder chain must run
for BOTH outcomes: duplicate the remaining chain into each branch, or place the If/Else after the whole
reminder chain if that reads cleaner, and say in the report which you did. Read back and list every node. Do
not touch its Publish switch.

## Part 48: the audit code
In "Audit: prep email", on the None branch before the send, set `audit_code` to the first three letters of the
company name in upper case plus YYMM (for example TEL2609). Use whatever text or math action this account has.
If GHL cannot build that string, create a task for David instead, "Set the audit code for <company>", and say
so. The prep email links to forms.atlasonesolutions.com/audit/?audit_code={{contact.audit_code}}, and an empty
code must still leave a working link, so check what that URL renders as when the field is blank and report it.

## Part 49: stop the offer workflow overwriting the real expiry date
Open "Audit: offer follow up" (73d4ded5-442c-48fc-a2ac-cee309924853). Run BQ set its "Set - Audit offer
expires" action to the right now date, because this account has no date math. That is now wrong: GHL-JOBS Run
BT makes the Audit report itself write the true date (report date plus 30) to Audit Offer Expires before David
tags the contact, and the two offer emails no longer print a date at all. So DELETE the "Set - Audit offer
expires" action from this workflow entirely, leaving the trigger straight into Wait 20 days. Nothing else in
the workflow changes. Read back the whole workflow and confirm the two email subjects are still exactly
"Ten days left on your Audit offer, {{contact.first_name}}" and "Your Audit offer closes in two days". Leave
it Draft.

## Part 50: Form D check (expected answer: no Form D)
Read Form E "Atlas One - Audit intake" (pUCVA3wZgAOMMVnZsb4c) first. It should already carry name, email,
company, phone, the four optional uploads, the staff pulse checkbox and the hidden audit code. If it does,
build no second form and record "Form E covers it". If anything is missing, add it to Form E rather than
building Form D. Never add an SSN, bank number or date of birth field. Then open "Audit: intake received"
(ba3f3f1b-1ae1-4dcd-8d96-2b79e3d6cf87) and add, alongside what it already does: Add Tag `audit-docs-received`,
and a task for David "Audit files in: pre-run the Workbench before the call" due the day before the
appointment if that date is available in this workflow, otherwise due same day (say which). Nothing chases a
prospect for missing files.

## Part 51: the 90 day re-pulse, then verify
New workflow "Client: 90 day pulse", Draft. Trigger Contact Tag added = `client-current`. Wait 90 days. Task
for David "Send the 90 day staff pulse link for {{contact.company_name}}"; put the pulse admin URL in the task
body only if `PULSE_ADMIN_URL` in `audit-client-template-id.md` is not empty (it is empty today, so the task
alone). END.
Then verify: one test contact with a unique phone and email, booked through the 30 minute calendar three
times, once per answer, and confirm the right email arrives each time and only that one. A second test contact
tagged `client-current` gets the client email whatever answer it picks and never the prep email. One Form E
submission lands the files, the tag and the task. Shorten the offer follow up Waits to test, then restore them
exactly and read them back. Send nothing to a real prospect. Delete no test contact (David does).

## Report
First line: what David must publish, and anything a human has to click that automation could not. Then per
part: what was done, read backs, screenshots, assumptions, "Questions for David" at the end.
