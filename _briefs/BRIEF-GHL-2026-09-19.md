# BRIEF-GHL (current run: Run BW, 2026-09-19 17:30). DONE: Parts 1 to 53. THIS RUN: Part 56, check David's three hand edits, then Part 57, the live verification that Runs BS and BV never finished.

History for older runs is in this repo's `_briefs/` copies and in each `RUN-*-report.md`.

Terminal rules unchanged: ghl-browser only, start at app.ridethehightide.com/, never a deep URL, one low stakes
click first, stop cleanly after one Cmd+R if it does not land. Never fork or background (rule 44). Fresh
navigation before trusting state (rule 42). Never touch a Publish or Draft switch. Send nothing to a real
prospect, no SMS, no HIPAA, delete no files, spend nothing. No dashes. Assets `_briefs/assets/run-BW/`.

## What may be deleted in this run, and nothing else
Only these two, and only if they are still present:
1. the step "Current client?" inside "Booking: confirm and remind" (7200e594-0a7a-47cc-847e-cb8a8e39eb31)
2. the action "#1 Task - set audit code" inside "Audit: prep email" (f9162dd7-692e-4f91-80af-76c4d034ccb6)
Both are Published, so the permission classifier will likely deny the delete. Try each ONCE. If denied, do not
retry, do not work around it, write it at the top of the report as David's click and carry on. Deleting
anything else, a contact, an appointment, a workflow, a form, a file, is forbidden in this run.

## Part 56: read back what David did by hand
Open both workflows above by fresh navigation and list every node.
- "Booking: confirm and remind" should read: trigger (Book time with... calendar group), Internal
  Notification, Email C1 Booking confirmation, Add Tag booked, Wait until 24 hours before, Email 24h reminder,
  Wait until 1 hour before, Email Talk in an hour, END. Nothing named "Current client?".
- "Audit: prep email" should read: trigger, If/Else Client?, Client branch END, None branch Wait 10 minutes,
  Email prep, END. No "#1 Task - set audit code".
Also read the workflow list and record the Published or Draft state of: Booking: confirm and remind, Audit:
prep email, Audit: intake received, Audit: offer follow up, Booking: client services email
(817ee224-8e98-4142-b72d-c7aa4a2d38dd), Client: 90 day pulse (52bf136f-2f90-4524-bc8a-8851fd1b3052).
Screenshot each. If either edit is still outstanding, say so plainly in the first line of the report.

## Part 57: the live verification
Run whatever of this the publish states allow, and say clearly which tests were skipped and why. Test contacts
get a unique phone and email; delete none of them afterwards.
a. A contact tagged `client-current` books the 30 minute calendar and answers "My Back Office Audit". Expect:
   booking confirmation, the client services email within about 5 minutes, and NO prep email. This is the test
   that proves the whole fix. It only works if "Booking: client services email" is Published; if it is Draft,
   say so and skip.
b. A contact with no tag answers "My Back Office Audit". Expect: confirmation plus the prep email about ten
   minutes later, and no client services email.
c. A contact answers "Something else". Expect: confirmation and reminders only.
d. One live Form E submission with a small test file. Expect: the file on the contact, tags `audit-intake` and
   `audit-docs-received`, and both tasks.
e. "Audit: offer follow up": shorten the Waits, run one contact through, restore the Waits exactly, read back.
Record for each test what actually arrived, from the contact's own activity, not from what the workflow looks
like it should do.

## Report
First line: whether the two hand edits are done, and what David still has to click or publish. Then per test:
what was sent, what arrived, what did not, screenshots, assumptions, "Questions for David" at the end.

## Part 58 (added 2026-09-19 17:40): the two workflows both named "Booking: after the call", READ ONLY
David found two workflows with the same name, one Published and one Draft. Do not delete, rename, publish or
edit either one. Open each by fresh navigation and write down, for both: the workflow id, the Published or
Draft state, the trigger with every filter, every node in order with its settings (template name, subject,
From name and address, tag names, wait lengths, task text), the Created and Last updated dates if the list
shows them, and the enrollment or execution counts if the Enrollment history or Execution logs tabs show any.
Screenshot each canvas at Fit to Screen.
Then say plainly in the report which of the two is the real one and which looks like a stray copy, and on what
evidence (contacts enrolled, last updated, whether one is an exact subset of the other, whether either was
built by a run in this repo's `_briefs/` history). Recommend one of: retire the Draft, retire the Published one
and publish the Draft, or merge, and name the exact clicks David would make. He decides and clicks. Nothing in
this part changes anything in GoHighLevel.
