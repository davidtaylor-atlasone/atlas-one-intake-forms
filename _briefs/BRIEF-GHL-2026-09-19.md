# BRIEF-GHL (current run: Run BX, 2026-09-19 18:30). DONE: Parts 1 to 58. THIS RUN: Part 59 fix the AND/OR gate, Part 60 prove it with a live booking, Part 61 the tests that keep getting skipped, Part 62 read the never-used after-the-call workflow.

History for older runs is in this repo's `_briefs/` copies and in each `RUN-*-report.md`.

Terminal rules unchanged: ghl-browser only, start at app.ridethehightide.com/, never a deep URL, one low
stakes click first, stop cleanly after one Cmd+R if it does not land. Never fork or background (rule 44).
Fresh navigation before trusting state (rule 42). Never touch a Publish or Draft switch. Send nothing to a
real prospect, no SMS, no HIPAA, delete no files, spend nothing. No dashes. Assets `_briefs/assets/run-BX/`.
Answers to Run BW's questions: (1) fix it this run, Part 59. (2) do not delete it, read it first, Part 62.
(3) and (4) yes, run them, Part 61. (5) noted, not this run, it came from a fake example.com address.

## Nothing may be deleted in this run
No contacts, no appointments, no workflows, no forms, no files, no nodes. The one exception is Part 59's
Segment 2 inside one condition, if that is the route taken. Everything else is edit or read only.

## Part 59: the actual bug, one condition
Run BW proved it from the Execution logs: "Booking: client services email"
(817ee224-8e98-4142-b72d-c7aa4a2d38dd, Published) has its "Current client?" gate joined with AND, so a contact
must carry the `client-current` tag AND have answered "I am already a client". A current client booking the
Audit calendar picks the audit reason, so the gate is false and no client email ever sends. "Audit: prep
email" has the same gate joined with OR, which is why the suppression half works.
Open that workflow and make its gate read exactly like "Audit: prep email"'s: Segment 1 Tags includes
`client-current` OR Segment 2 Call purpose (`contact.call_purpose`) Is "I am already a client". Change the
join from AND to OR if the panel allows it; if it does not, delete Segment 2 and leave the tag alone, which
gives the same behaviour for every real case. Save. Read back by fresh navigation and screenshot the condition
with the OR visible. The workflow is already Published, so Save is the whole go-live; do not touch the switch.

## Part 60: prove it with a real booking, not by looking at the canvas
Fresh test contact, unique phone and email, tag `client-current`, booked through the public 30 minute Audit
widget answering "My Back Office Audit" (the same answer that failed in Run BW). Then read the Execution logs
of both workflows and the contact's own activity. Pass means: the client services email actually sent about
5 minutes later, and no prep email sent. Quote the condition trace from the log the way Run BW did. If it
still does not send, stop, write what the trace says, and do not start guessing at fixes.

## Part 61: the three tests that keep getting skipped
Use fresh test contacts each time, unique phone and email, and read results from Execution logs and contact
activity, never from the canvas.
a. No tag, answers "My Back Office Audit": expect the booking confirmation, then the prep email about ten
   minutes later, and NO client services email.
b. No tag, answers "Something else": expect the confirmation and the reminder chain only, nothing else.
c. One live Form E submission with a small test file: expect the file on the contact, tags `audit-intake` and
   `audit-docs-received`, and both tasks created.
If the sandbox permission gate denies a click twice in a row on any one of these, skip that test, say so, and
move to the next rather than ending the run. Do not retry a denied click more than once.

## Part 62: "Booking: after the call" (d928c199-70af-4134-9bc2-000cfe7d2b30), read only
Run BW recommended retiring this as a stray. Cowork disagrees and wants it read properly first: a follow up
after every Audit call is exactly the thing that turns an audit into a client, and this workflow being Draft
with zero enrollments may mean it was built and never switched on, not that it is junk.
Read and report: every node in order with its settings, the exact subject and body of the emails it sends
(Email C2 After the call, Email C3 Soft follow up), whether those templates still exist and carry the current
phone 380-225-5217 and the current booking link, whether any copy in them contains a dash, whether its trigger
filters are still correct for the 30 minute Audit and 15 minute Intro calendars, and whether anything in it
overlaps or conflicts with "Audit: offer follow up" or the prospecting cadences. Then state plainly whether it
is safe for David to publish as is, safe after named fixes, or genuinely redundant. Change nothing.

## Report
First line: whether the client services email now actually sends, proven from a log, and anything David must
click. Then per part: what was done, what the logs said, screenshots, assumptions, "Questions for David" at
the end.
