# BRIEF-GHL (current run: Run BY, 2026-09-19 22:20). DONE: Parts 1 to 62, the client services email is proven working. THIS RUN: Parts 63 to 66, three small corrections Run BX surfaced, then prove them.

History for older runs is in this repo's `_briefs/` copies and in each `RUN-*-report.md`.

Terminal rules unchanged: ghl-browser only, start at app.ridethehightide.com/, never a deep URL, one low
stakes click first, stop cleanly after one Cmd+R if it does not land. Never fork or background (rule 44).
Fresh navigation before trusting state (rule 42). Never touch a Publish or Draft switch. Send nothing to a
real prospect, no SMS, no HIPAA, delete no files, spend nothing. No dashes. Assets `_briefs/assets/run-BY/`.
Remember Run BX's lesson: on a Published workflow, saving the action is not enough, the top level workflow
Save must be clicked too, then read back after a full reload.

Answers to Run BX's questions, decided, do not re-ask: (1) yes, narrow it, Part 63. (2) publish it, but after
Part 64's fix; David clicks Publish, not the terminal. (3) yes, rename it, Part 65.

## Nothing may be deleted in this run
No contacts, no appointments, no workflows, no forms, no files. Adding a branch or a segment is fine.

## Part 63: stop "Something else" bookings getting the Audit document request
Run BX proved it: a contact who books the 30 minute Audit calendar and answers "Something else" still gets
"Before our Audit call: four documents". They did not come for an Audit, so asking them for bank statements
and a workers comp declarations page reads as a mistake.
In "Audit: prep email" (f9162dd7-692e-4f91-80af-76c4d034ccb6, Published), widen the existing "Client?"
condition rather than adding a second branch: Segment 1 Tags includes `client-current` OR Segment 2 Call
purpose Is "I am already a client" OR Segment 3 Call purpose Is "Something else", all joined with OR. That
branch already ends the workflow, so those three cases get nothing extra and everyone else still gets the prep
email. A blank or missing answer must still receive the prep email, so never invert this into an "is audit"
test. Rename the condition to "Skip prep email?" if the panel allows a rename, otherwise leave the name.
Save the action, then Save the workflow, then read back after a full reload and screenshot the three segments.

## Part 64: close the gap in "Booking: after the call" before it goes live
d928c199-70af-4134-9bc2-000cfe7d2b30, Draft. Its "Next step already sent?" gate checks tags `form-a-sent`,
`form-b-sent` and `proposal-sent`. It is missing the one that matters most now: a prospect who has already
received their Audit report carries `audit-offer-sent`, and sending them "Still thinking it over?" three days
later is wrong. Add a fourth segment to that gate, Tags includes `audit-offer-sent`, joined with OR like the
others, so those contacts take the Yes branch and end. Change nothing else in the workflow. Save, read back
after a full reload, screenshot the four segments. Leave it Draft, David publishes.

## Part 65: the calendar name with the em dash
In Calendars settings, the 15 minute calendar is named "Atlas One -- 15-Minute Intro Call" with an em dash in
the name itself, which shows to prospects on the booking page and in confirmations. Rename it to
"Atlas One 15 Minute Intro Call". Do not change its slug, its URL, its availability, its team members or any
other setting, and do not touch the other three calendars. If renaming would change the booking URL, stop and
say so instead, since that link is in the email signature and on the website. Read back and screenshot.

## Part 66: prove Parts 63 and 64
a. Fresh test contact, no tag, unique phone and email, books the 30 minute Audit calendar answering
   "Something else". Pass means: booking confirmation arrives, and ten minutes later NO prep email. Read it
   from the contact's activity and the workflow's Execution log condition trace, not the canvas.
b. Fresh test contact, no tag, books answering "My Back Office Audit". Pass means the prep email still
   arrives about ten minutes later. This proves Part 63 did not break the normal path.
c. For Part 64, no live test is possible without publishing, so instead read the gate's four segments back
   after a reload and state plainly that it is verified by read back only.
If the sandbox permission gate denies a click twice on any one test, skip that test, say so, carry on.

## Report
First line: whether a "Something else" booking now gets nothing, proven from a log, and what David must
publish. Then per part: what was done, what the logs said, screenshots, assumptions, "Questions for David" at
the end. Also list every ZZTest contact and test appointment created across Runs BS to BY in one place, so
David can clear them in one pass.
