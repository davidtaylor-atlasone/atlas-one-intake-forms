# BRIEF-GHL (current run: Run BU, 2026-09-18 22:45). DONE: Parts 1 to 51. THIS RUN: Parts 52 to 55. Fix the client email timing the right way, drop the audit code busywork, finish the verification Run BS could not.

History for older runs is in this repo's `_briefs/` copies and in each `RUN-*-report.md`.

Terminal name: GHL. Browser work in GoHighLevel through the ghl-browser Playwright MCP only. David restarts
Chrome and the extension before this run because Run BS ended with the tool unresponsive; if ghl-browser still
hangs on the first low stakes click after one Cmd+R, stop cleanly, write "needs Chrome restart" at the top of
the report and end. Start at app.ridethehightide.com/, never a deep URL. Never fork or background (rule 44).
Fresh navigation before trusting state (rule 42). Never touch a Publish or Draft switch. Branches do not merge
(rule 35). Send nothing to a real prospect, no SMS, no HIPAA, delete no files, spend nothing. No dashes in any
copy. Every part ends with a fresh navigation read back and a screenshot. Assets `_briefs/assets/run-BU/`.

## Why this run exists
Run BS put the "Current client?" If/Else at the END of "Booking: confirm and remind", after both reminder
waits, so the client email would not arrive until the day of the appointment. That was Cowork's brief offering
a bad alternative, not the terminal's error. The fix is not to rebuild that branch in a published workflow.
The client email gets its own workflow, exactly like "Audit: prep email" already does, and the published
booking workflow goes back to the eight nodes it has always had.

David removes the bad node himself before this run starts (a published workflow is what the permission
classifier keeps blocking). Part 52 is a check, not a build.

## Part 52: confirm the booking workflow is back to normal
Open "Booking: confirm and remind" (7200e594-0a7a-47cc-847e-cb8a8e39eb31). Read every node after a fresh
navigation. It must be exactly: trigger (Book time with... calendar group), Internal Notification, Email C1
Booking confirmation, Add Tag booked, Wait until 24 hours before, Email 24h reminder, Wait until 1 hour
before, Email Talk in an hour, END. No "Current client?" step anywhere. If it is still there, do NOT delete
it: write at the top of the report that David still needs to remove it, skip to Part 53, and build nothing
into this workflow. Do not touch its Publish switch either way.

## Part 53: the client email as its own workflow
New workflow "Booking: client services email", left in Draft. Trigger: Customer booked appointment, In
calendar = 30-Minute Back-Office Audit (the single calendar, same filter "Audit: prep email" uses, not the
group). First action an If/Else "Current client?": the Client branch is Tags includes `client-current` OR Call
purpose (`contact.call_purpose`) Is "I am already a client"; the None branch goes straight to END. On the
Client branch: Wait 5 minutes, then Send Email, template `A1 | Client | more we handle`
(6aad97b6cbcc9427cbd4f68b), From Name "David Taylor, Atlas One Solutions", From Email
David@AtlasOneSolutions.com, subject "Everything Atlas One handles for you now, {{contact.first_name}}", then
END. The 5 minute wait is so it lands after the booking confirmation, not on top of it. Nothing else goes in
this workflow: the reminders stay where they are and are not duplicated anywhere.

## Part 54: drop the audit code task
Run BS proved GHL cannot build the code (no formatter action in this account can read another action's
output), and it confirmed the prep email link still works with an empty code. A task on every single audit
booking is busywork David does not need, so remove it: open "Audit: prep email"
(f9162dd7-692e-4f91-80af-76c4d034ccb6) and delete the Add task action "Set the audit code for ..." from the
None branch. That workflow is Draft, so the classifier should allow it; if the delete is blocked anyway, leave
it and say so plainly in the report rather than retrying more than twice. The branch must then read: trigger,
If/Else Client?, None branch Wait 10 minutes, Email prep, END. The audit code is assigned by the Audit
Workbench when David runs the audit, not by GHL.

## Part 55: finish the verification Run BS could not
Only after 52 to 54 read back clean. Use test contacts with unique phone and email, never a real prospect, and
delete nothing afterwards (David does that).
a. A contact tagged `client-current` books the 30 minute calendar and answers "My Back Office Audit": it must
   still get the client email from the new workflow and must NOT get the prep email.
b. A contact answering "Something else": booking confirmation and reminders only, nothing extra.
c. A live Form E submission: files land, tag `audit-intake`, tag `audit-docs-received`, and both tasks appear.
d. "Audit: offer follow up": shorten the Waits, run it through, then restore them exactly and read them back.
e. Note in the report which of the four Draft workflows David still has to publish.
Note: the workflows built in Run BS are Draft, so a booking test only proves the prep email path if David has
published "Audit: prep email". If it is still Draft, say so and test what can be tested rather than waiting.

## Report
First line: whether the booking workflow is clean, and what David must publish or click. Then per part: what
was done, read backs, screenshots, assumptions, "Questions for David" at the end.
